version: '3'

services:
  postgres:
    image: postgres:15.3-alpine
    environment:
      - POSTGRES_USER=user
      - POSTGRES_DB=customerdb
      - POSTGRES_PASSWORD=password
    ports:
      - "5432:5432"